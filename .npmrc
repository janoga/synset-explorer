# Enable workspace hoisting for better performance
hoist=true

# Use node-modules linker for better IDE support
node-linker=hoisted

# Automatically install peer dependencies
auto-install-peers=true

# Use exact versions when saving dependencies
save-exact=false
